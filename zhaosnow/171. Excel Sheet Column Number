//没啥好说的
class Solution {
public:
    int titleToNumber(string s) {
      int a = 'A' - 1;
      int sum = 0;
      for(int i = 0; i < s.size(); i ++){
          sum = sum * 26 + s[i] - a;
      }
      return sum;
    }
};
